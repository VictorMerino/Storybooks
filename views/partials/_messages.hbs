{{#if errors}}
  <div class="errors">
    <ul class="people_list collection">
      {{#each errors}}
        <li class="collection-item red-text">{{this.msg}}</li>
      {{/each}}
    </ul>
  </div>
{{else}}
  <h1>No errors</h1>
{{/if}}


